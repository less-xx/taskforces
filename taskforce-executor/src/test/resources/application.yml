spring:
  application:
    version: ^project.version^
    name: ^project.name^
  banner.location: classpath:/banner.txt
  
taskforce:
  id: test-taskforce-id
  storage-provider: org.teapotech.taskforce.provider.RedisTaskforceStorageProvider
  
taskforce.execution.driver: docker